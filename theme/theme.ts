// App-shell colors (header, page background, scroll fade). The calculator
// content itself uses the flat palette in ledTheme.ts.
export const colors = {
  background: '#02080f',
  text: '#ffffff',
};

export const fonts = {
  regular: 'Roboto_400Regular',
  bold: 'Roboto_700Bold',
};

// 4-space base unit grid — every margin/padding is a multiple of this.
export const space = {
  xs: 4,
  sm: 8,
  md: 16,
  lg: 24,
  xl: 32,
};
